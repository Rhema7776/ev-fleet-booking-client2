# Produced inside the image, never copied in.
node_modules
dist
dist-ssr

# VCS
.git
.gitattributes
.gitignore
.github

# Docker
Dockerfile
.dockerignore

# Local env — must not be baked into an image layer.
.env
.env.*
*.local

# Editors / OS
.vscode
.idea
.DS_Store
Thumbs.db

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Not needed by the build; excluded so touching them does not bust the cache.
README.md
.oxlintrc.json
components.json
