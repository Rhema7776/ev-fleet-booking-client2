// Placeholder — the nav item exists (matching the real Figma design),
// but no wallet page was ever built. Not invented here either, beyond a
// real, working, empty stub — the actual wallet UI (lease hour balance,
// funding, transaction history, etc.) is a separate, bigger task.
const Wallet = () => {
  return <div>Wallet</div>;
};

export default Wallet;
